# TODO - Fix fine_count usage in store_return and ReturnController

## Tasks:
- [x] Fix store_return stored procedure to use total_price function first, then fine_count
- [ ] Fix ReturnController.php update method to use total_price function first, then fine_count
